// Mocking Firebase config to avoid build errors due to missing modules or incorrect environment setup.
// This service is currently unused in the application main flow.

export const auth = {} as any;
export const db = {} as any;

const app = {} as any;
export default app;
